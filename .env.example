# Supabase project credentials (Phase 2 — auth & cloud sync).
# Create a project at https://supabase.com, then copy this file to .env.local
# and fill in the values from Project Settings → API.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
